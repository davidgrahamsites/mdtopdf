name: mdtopdf-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - PyQt6>=6.6.0
      - PyQt6-WebEngine>=6.6.0
      - markdown>=3.5.0
      - pymdown-extensions>=10.5
      - pygments>=2.17.0
      - weasyprint>=60.0
      - Pillow>=10.1.0
      - pyinstaller>=6.3.0
